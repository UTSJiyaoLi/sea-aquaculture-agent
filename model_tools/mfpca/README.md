# MFPCA Placeholder

`Data/surface_data_new_nodof.csv` and `MFPCA_paper.R` are retained as raw references for future spatial water-quality analysis.

They are not part of the V1 request path. A future `WaterQualityMfpcaTool` can consume them as an optional analysis step or offline preprocessing workflow.
